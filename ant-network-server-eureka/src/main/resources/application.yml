server:
  port: ${PORT:2001}

spring:
  application:
    name: ant-network-eureka-server

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    wait-time-in-ms-when-sync-empty: 0

